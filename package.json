{
  "name": "convertri-security-worker",
  "version": "1.0.0",
  "description": "Cloudflare Worker to add security headers to Convertri pages",
  "main": "worker.js",
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev"
  },
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
}


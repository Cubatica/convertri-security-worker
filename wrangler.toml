name = "convertri-security-worker"
main = "worker.js"
compatibility_date = "2024-01-01"

# Routes for the domain
routes = [
  { pattern = "article.wheelofcharlotte.com/*", zone_name = "wheelofcharlotte.com" }
]

# Or use workers.dev subdomain (for testing)
# workers_dev = true

